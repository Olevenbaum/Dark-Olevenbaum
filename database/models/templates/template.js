// Importing classes
const { DataTypes } = require("sequelize");

module.exports = (sequelize) => {
    // Defining model
    return sequelize.define(
        "",
        {},
        // Options
        {}
    );
};
