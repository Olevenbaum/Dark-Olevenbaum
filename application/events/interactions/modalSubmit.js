// Importing classes and methods
const { InteractionType } = require("discord.js");

module.exports = {
    // Setting interaction type name
    name: InteractionType.ModalSubmit,

    // Handling interaction
    async execute(interaction) {},
};
